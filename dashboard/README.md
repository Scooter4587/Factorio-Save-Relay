# Dashboard

The dashboard is intentionally deferred until the sync core is proven. Its first
version will show world status, the active host lease, five retained revisions,
restore actions and an explicit emergency unlock.
